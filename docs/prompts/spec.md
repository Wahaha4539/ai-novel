# Prompt 模板规范

## 目录约定

- `chapter/`：章节/场景正文生成
- `planning/`：分卷/章节/场景规划
- `validation/`：前置/后置校验
- `summary/`：摘要与事件提取

## 模板原则

1. 模板只描述写作约束与输出格式，不绑定业务代码。
2. 所有动态数据通过 Prompt Builder 注入，不在模板中硬编码业务字段。
3. 模板修改需要记录版本，以便追溯生成差异。
4. 生成正文模板必须明确“只输出正文”。
